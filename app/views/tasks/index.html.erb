<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to(task.id,"/tasks/#{task.id}") %> : <%= task.content %> &gt; <%= task.status %>
    </li>

  <% end %>
</ul>

<%= link_to('新規メッセージの投稿',"/tasks/new") %>
